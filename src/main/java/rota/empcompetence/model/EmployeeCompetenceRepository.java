package rota.empcompetence.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EmployeeCompetenceRepository extends JpaRepository<EmployeeCompetenceVO, EmployeeCompetenceId> {
}
